#!/usr/bin/env bash

#MISE description="Build the application (Debug)"
#MISE sources=["Cargo.toml", "crates/**/*.rs"]
#MISE outputs=["target/debug/hodor"]

proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

set -euo pipefail

cargo build --quiet --workspace "$@"
