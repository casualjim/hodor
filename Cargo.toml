
[package]
name = "hodor"
version = "0.1.19"
edition = "2024"
description = "grant-scoped MITM proxy"
license = "Apache-2.0"
repository = "https://github.com/casualjim/hodor"
keywords = ["mitm", "proxy", "tls"]
categories = ["network-programming"]

[package.metadata.dist]
# hodor is a single static-friendly binary; container + tarballs ship from CI.

[package.metadata.release]
sign-commit = false
sign-tag = false
publish = false
allow-branch = ["main"]
pre-release-commit-message = "chore: Release {{crate_name}} version {{version}}"
[workspace]
members = ["crates/*"]
# `hodor-ebpf-programs` is a member (so `aya-build` can resolve it with
# `cargo build --package`) but never part of a host build: it only compiles for
# the `bpf` target with nightly and `-Z build-std`, so every workspace-wide
# command excludes it — see `hk.pkl` and `.mise/tasks/test/rust`.
resolver = "3"

[workspace.lints.rust]
ambiguous_negative_literals = "warn"
missing_debug_implementations = "warn"
missing_docs = "warn"
redundant_imports = "warn"
redundant_lifetimes = "warn"
trivial_numeric_casts = "warn"
unsafe_op_in_unsafe_fn = "warn"
unused_lifetimes = "warn"

[workspace.lints.clippy]
allow_attributes_without_reason = "warn"
as_pointer_underscore = "warn"
assertions_on_result_states = "warn"
cargo = { level = "warn", priority = -1 }
cast_precision_loss = "allow"
clone_on_ref_ptr = "warn"
complexity = { level = "warn", priority = -1 }
correctness = { level = "warn", priority = -1 }
deref_by_slicing = "warn"
disallowed_script_idents = "warn"
empty_drop = "warn"
empty_enum_variants_with_brackets = "warn"
empty_structs_with_brackets = "warn"
float_cmp = "allow"
fn_to_numeric_cast_any = "warn"
if_then_some_else_none = "warn"
implicit_clone = "warn"
literal_string_with_formatting_args = "allow"
map_err_ignore = "warn"
multiple_crate_versions = "allow"
pedantic = { level = "warn", priority = -1 }
perf = { level = "warn", priority = -1 }
redundant_type_annotations = "warn"
renamed_function_params = "warn"
semicolon_outside_block = "warn"
style = { level = "warn", priority = -1 }
suspicious = { level = "warn", priority = -1 }
undocumented_unsafe_blocks = "warn"
unnecessary_safety_comment = "warn"
unnecessary_safety_doc = "warn"
unneeded_field_pattern = "warn"
unused_result_ok = "warn"

[dependencies]
clap = { version = "4", features = ["derive", "env"] }
confique = { version = "0.4", features = ["toml"] }
dirs = "6"
eyre = "0.6"
hodor-compose = { path = "crates/hodor-compose" }
hodor-config = { path = "crates/hodor-config" }
hodor-fnox = { path = "crates/hodor-fnox" }
hodor-pki = { path = "crates/hodor-pki" }
hodor-proxy = { path = "crates/hodor-proxy" }
miette = { version = "7", features = ["fancy"] }
mimalloc = "0.1"
tokio = { version = "1", features = ["io-util", "macros", "net", "parking_lot", "rt-multi-thread", "sync", "time"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

[target.'cfg(target_os = "linux")'.dependencies]
hodor-ebpf = { path = "crates/hodor-ebpf" }
hodor-tproxy = { path = "crates/hodor-tproxy" }
hodor-tun = { path = "crates/hodor-tun" }

[dev-dependencies]
pretty_assertions = "1"
tempfile = "3"

[lints]
workspace = true

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
